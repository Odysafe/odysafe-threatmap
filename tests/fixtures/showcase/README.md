# Simulated Showcase Dataset

All assets in this directory are test-only simulated data. Production commands
never load this directory automatically. The fixture is reserved for exercising
the same ATT&CK repository, application services, and Excel exporters used by
production with a visually dense, deterministic dataset.
